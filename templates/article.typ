#LANGUAGE(stack(
    dir: ttb,
    [= TITLE ],
    spacing,
    HEADER
    balance(columnar[
        BODY
    ]),
    spacing,
    FOOTER
    v(0.5em),
    align(right, author(\"KÜRZEL\")),
))

#pagebreak()
